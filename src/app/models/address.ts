export interface Address {
    streetNumber: string;
    street: string;
    city: string;
    zipCode: string;
    state: string;
}

export interface SortAddress {
    active: string;
    direction: string;
}
