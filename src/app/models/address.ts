export interface Address {
    streetNumber: string;
    street: string;
    city: string;
    state: string;
    zipCode: string;
}

export interface SortAddress {
    active: string;
    direction: string;
}
